= render 'shared/main-header'
- breadcrumb :logout
= render "shared/breadcrumbs"
%main.card-main
  = render 'side-bar'
  .card
    .logout-content
      .logout-content__in
        = link_to "ログアウト",destroy_user_session_path, method: :delete, class: 'logout-content__in'
= render 'shared/main-footer'
= render "shared/red-camer"